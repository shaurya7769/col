{
  "name": "skate-cms-monorepo",
  "version": "1.0.0",
  "description": "Orchestrated social platform for skate academies",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm run build --prefix frontend && serve -s frontend/dist\"",
    "verify": "node backend/scripts/verify.js",
    "test": "npm test --prefix backend && npm test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
